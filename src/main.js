import { createApp } from "vue";

import App from "./App.vue";

const test = {
	name: "age",
	age: 12,
};

createApp(App).mount("#app");
const testObj = {
	a: "nam",
};
const str = 'sdfsdt'
const add = 'add'
console.log('再次修改')